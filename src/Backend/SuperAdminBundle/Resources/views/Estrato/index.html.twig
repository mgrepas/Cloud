<h1>Estrato list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Estrato</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('dashboard_estrato_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.estrato }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('dashboard_estrato_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dashboard_estrato_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('dashboard_estrato_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
